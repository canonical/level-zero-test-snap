#!/bin/bash

set -euo pipefail

BIN_DIR="${SNAP}/usr/bin"

echo "Usage: level-zero-tests.test <test-name> [args...]"
echo ""
find "${BIN_DIR}" -maxdepth 1 -type f -perm -111 -name "test_*" -printf "%f\n" | sort
