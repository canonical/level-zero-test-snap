#!/bin/sh

set -e

# Under strict confinement we cannot write to /etc. Keep the hook minimal
# and snap-local.
snapctl set agent=enabled || true

echo "level-zero-tests: install hook completed (strict confinement)."
